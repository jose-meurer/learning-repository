// for - classico

const frutas = ['Maça', 'Banana', 'Pêra', 'Uva', 'Abacaxi', 'Melancia', 'Mamão', 'Morango'];

//      i = index
for(let i = 0; i < frutas.length; i++) {
    console.log(`Índice ${i} ${frutas[i]}`);
}