//String
const nome = "José";
const nome2 = 'José';
const nome3 = `José`;
console.log(nome, typeof nome);

//Number
const num1 = 10;
const num2 = 15.32;
console.log(num1, typeof num1);

//Undefined and Null
let nomeAluno; //undefined = Não aponta para local nenhum da memória
let sobrenomeAluno = null; //Nulo = Tambem nao aponta para local nenhum da memória;
console.log(nomeAluno, typeof nomeAluno);
console.log(sobrenomeAluno, typeof sobrenomeAluno);

//Booleano
const aprovado = false;
console.log(boolean, typeof boolean);
 
/**
 * Tipos primitivos:
 * string
 * number
 * undefined
 * null
 * boolean
 * symbol
 */